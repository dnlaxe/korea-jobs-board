{{! views/jobs/start.hbs }}
<div class="flex flex-1 items-center justify-center px-4 py-8">

  {{#if actionError}}
    <p
      class="text-red-500 text-sm border border-red-500 py-1 px-2 rounded inline-block"
    >{{actionError}}</p>
  {{/if}}

  <form action="/jobs/start" method="POST" class="w-full max-w-xl mb-24 mx-4">
    <div>
      <h1 class="text-3xl mb-6 font-semibold">Let's create your job post</h1>
    </div>
    <div>
      <label class="block mb-2" for="email">First, we need you email address</label>
      <p class="mb-2">We'll only use this to send you a receipt</p>
      <input
        class="block w-full px-3 py-2 rounded border bg-white"
        id="email"
        name="email"
        type="email"
        required
        value="{{values.email}}"
      />
      {{#if fieldErrors.email}}
        <div
          class="text-sm text-red-500 mt-1"
          data-error="email"
        >{{fieldErrors.email.[0]}}</div>
      {{/if}}
    </div>
    <div class="flex mt-4">
      <button
        type="submit"
        class="border border-slate-200 py-2 px-3 font-mono uppercase text-sm tracking-wide hover:border-black ml-auto"
      >Continue</button>
    </div>
  </form>
</div>